// This module is deprecated and replaced by the new bookings, contact, and auth modules.
// Kept as a stub to prevent build errors from legacy imports.
import { Router } from 'express';
const router = Router();
export default router;
